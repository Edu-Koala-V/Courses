# 6.1 Wprowadzenie do PowerShell

## Wprowadzenie

W czasach rosnącej popularności języków programowania wspierających podejście obiektowe oraz potrzeby administratorów do zaawansowanego zarządzania systemem przez wiersz poleceń, **_Microsoft_** stworzył _**PowerShell**_, czyli nowoczesną powłokę poleceń i język skryptowy.

_**PowerShell**_ opiera się na paradygmacie obiektowości, co czyni go bardziej podobnym do języków programowania, takich jak `Python` czy `C#`, niż do archaicznego, choć wciąż używanego, Wiersza polecenia (**CMD**) i jego języka skryptowego **BATCH**.

### Dlaczego uczyłeś się CMD i BATCH?

- Ponieważ **CMD** oferuje proste, nadal stosowane polecenia, takie jak `ipconfig` czy `dir`, które są szybkie i skuteczne w codziennej pracy. 
- Skrypty **BATCH** umożliwiają podstawową automatyzację i strumieniowanie danych, co jest przydatne w prostych scenariuszach. 

_**PowerShell**_ idzie jednak o krok dalej, nadbudowując prostotę **CMD** potęgą obiektowego programowania, co pozwala na zaawansowaną automatyzację i zarządzanie systemem.


## PowerShell ISE - Twoje środowisko pracy

**PowerShell ISE** (*Integrated Scripting Environment*) to narzędzie stworzone dla skrypterów, które oferuje:
  - Edytor kodu z podświetlaniem składni.
  - Możliwość zapisywania skryptów w plikach `.ps1`.
  - Wbudowane narzędzia do debugowania (np. punkty przerwania).
  - Okno poleceń do testowania komend.

Od teraz w tym kursie będziemy pracować głównie w **PowerShell ISE**, aby tworzyć i testować skrypty w sposób wygodny i przejrzysty.


## Polityka wykonywania skryptów

Z powodów bezpieczeństwa _**PowerShell**_ domyślnie blokuje uruchamianie skryptów `.ps1`. Aby je włączyć, musisz zmienić politykę wykonywania:

```powershell
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned
```

  - `RemoteSigned`: Pozwala uruchamiać lokalne skrypty bez podpisu, ale wymaga podpisu dla skryptów zdalnych.
  - Aby sprawdzić aktualną politykę, użyj polecenia: 
  ```powershell
  Get-ExecutionPolicy -Scope CurrentUser
  ```
  Alternatywnie:
  ```powershell
  Get-ExecutionPolicy
  ```
  - Aby ponownie blokować uruchamianie skryptów `.ps1` użyj poniższej komendy:
  ```powershell
  Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy Restricted
  ```

<div data-hint="warning">

Zmiana polityki na `Unrestricted` jest ryzykowna, bo pozwala uruchamiać dowolne skrypty bez weryfikacji.  
Zdecydowanie bezpieczniej jest używać `RemoteSigned`.

</div>


## Pierwszy krok w PowerShell ISE

Zamiast przedstawiać ci oddzielnie polecenia użytkowe, a potem skrypty, w tym module od razu będziemy pisać skrypty i operować na poleceniach, wykorzystując ich wyniki.

Uruchom więc **PowerShell ISE** i napisz swój pierwszy skrypt. Następnie uruchom go w **ISE** oraz po zapisaniu jako plik `.ps1`, uruchom ten plik.

```powershell
Write-Output "Witaj świecie! 💪😎"
pause
```

Poniżej widzisz, że po zapisaniu skryptu wyskoczył błąd, więc zmieniłem politykę uruchamiania `.ps1` na `RemoteSigned`.  
Na czerwono zaznaczyłem opcję do uruchamiania skryptu `.ps1` oraz przycisk rozwinięcia okna edytora.

![Wynik polecenia Write-Output w PowerShell](https://raw.githubusercontent.com/Edu-Koala-V/images/main/Windows_11/Images/ps-first-time-run.webp)

---

# Podcast AI

<audio src="https://raw.githubusercontent.com/Edu-Koala-V/audio_podcast/refs/heads/main/Windows_11/Audio/PowerShell Essentials_ An Administrator's Introduction.wav" controls>
Twoja przeglądarka nie wspiera odtwarzania plików audio.
</audio>
# 6.3 Instrukcje warunkowe

## Wprowadzenie

Instrukcje warunkowe w _**PowerShell**_, podobnie jak `IF` w **BATCH**, pozwalają sterować działaniem skryptu na podstawie warunków.  
W _**PowerShell**_ oprócz standardowego bloku `IF ELSE` możesz użyć nowej, bardzo przydatnej instrukcji `SWITCH`.


## Podstawowa składnia

Instrukcje warunkowe w _**PowerShell**_:

| Instrukcja | Opis |
| :--- | :--- |
| `if` | Wykonuje kod, jeśli warunek jest prawdziwy. |
| `else` | Wykonuje kod, jeśli warunek w bloku `if` jest fałszywy. |
| `elseif` | Sprawdza kolejny warunek, jeśli poprzedni `if` jest fałszywy. |
| `switch` | Wykonuje kod dla pasującego przypadku (`case`), a jeśli żaden nie pasuje, opcjonalnie uruchamia przypadek domyślny (`default`). |

## Operatory porównania

W _**PowerShell**_ operatory porównania są oparte na języku angielskim i zawsze zaczynają się od myślnika.

| Operator | Opis | Przykład |
| :--- | :--- | :--- |
| `-eq` | Równość. | `$imie -eq "Janek"` |
| `-ne` | Nierówność. | `$imie -ne "Ania"` |
| `-gt` | Większe niż. | `$liczba -gt 10` |
| `-ge` | Większe lub równe. | `$liczba -ge 10` |
| `-lt` | Mniejsze niż. | `$liczba -lt 10` |
| `-le` | Mniejsze lub równe. | `$liczba -le 10` |
| `-like` | Dopasowanie wzorca z użyciem symboli wieloznacznych (np.: `*`, `?`). | `$imie -like "J*"` |
| `-notlike` | Niedopasowanie wzorca. | `$imie -notlike "A*"` |

-----

## Przykład: Skrypt z warunkiem `if`

Poniższy skrypt sprawdza imię użytkownika i wyświetla odpowiedni komunikat.  
Użycie `elseif` pozwala na sprawdzenie innych warunków.

```powershell
$imie = Read-Host "Podaj swoje imię"
if ($imie -eq "Janek") {
    Write-Output "Cześć, Janek! Jesteś adminem! 😎"
} elseif ($imie -eq "Ania") {
    Write-Output "Cześć, Ania! Jesteś użytkownikiem! 😄"
} else {
    Write-Output "Nieznane imię, spróbuj ponownie! 🤔"
}
Read-Host "Wciśnij Enter aby zakończyć działanie."
```

-----

## Przykład: Skrypt z warunkiem `switch`

Instrukcja `switch` jest idealna, gdy masz do czynienia z wieloma, precyzyjnymi wartościami dla **jednej zmiennej**.  
Jest wtedy bardziej czytelna niż wielokrotne użycie `if/elseif`, a klauzula `default` pozwala obsłużyć wszystkie przypadki, które nie pasują do żadnego zdefiniowanego.


```powershell
$imie = Read-Host "Podaj swoje imię"
switch ($imie) {
    "Janek" {
        Write-Output "Cześć, Janek! Jesteś adminem! 😎"
    }
    "Ania" {
        Write-Output "Cześć, Ania! Jesteś użytkownikiem! 😄"
    }
    default {
        Write-Output "Nieznane imię, spróbuj ponownie! 🤔"
    }
}
Read-Host "Wciśnij Enter aby zakończyć działanie."
```

---

# Podcast AI

<audio src="https://raw.githubusercontent.com/Edu-Koala-V/audio_podcast/refs/heads/main/Windows_11/Audio/PowerShell Conditional Statements_ IF, ELSE, SWITCH.wav" controls>
Twoja przeglądarka nie wspiera odtwarzania plików audio.
</audio>